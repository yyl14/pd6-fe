// Home page for displaying all the announcement

// requirements =>

// search bar
// button => add announcement
// list => displaying all the announcement with some features
// pageChange => changing the page
// rowDisplaying => displaying the amount of rows
import React, { Component } from 'react';

const AnnouncementHome = () => (
  <h1>hello</h1>
);

export default AnnouncementHome;
